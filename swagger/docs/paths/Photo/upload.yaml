post:
  tags: 
  - Photo
  summary: Upload image from client.
  description: Upload image from client.
  security: []
  requestBody:
    content:
      multipart/form-data:
        schema:
          type: object
          properties:
            file:
              type: string
              format: binary
  responses:
    200:
      description: OK
    400:
      description: Bad request
    500:
      description: Server error